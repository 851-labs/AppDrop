# Testing

appdrop targets 100% coverage on unit + fixture tests. Tests run locally via lefthook.

## Hooks

- `pre-commit`: unit + fixture tests
- `pre-push`: unit + fixture tests
- `pre-publish`: unit + fixture tests

## Run manually

```
bun test
```
